```
(defn square [num] (* num num))
square num = num * num
val square(x: Int): Int x => x * x
def square = { it * it }

(map square [1 2 3])
map square [1, 2, 3]
List(1, 2, 3).map(square)
[1, 2, 3].collect(square)
```
